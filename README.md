# GRPC Zap Log Middleware test

A simple grpc server/client implementation to test logging using go-grpc-middleware and zap log.

Intending to generate id for each request and have them output in the logs.

To run server: 
`go run main.go`

To run client
`cd client`
`go run main.go`